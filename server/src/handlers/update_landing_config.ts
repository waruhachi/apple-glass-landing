
// This handler has been removed as the landing page is now static
// and does not require backend configuration storage.
// The frontend will directly implement the design requirements.

export const updateLandingConfig = async (): Promise<never> => {
  throw new Error('Landing page configuration is now static and cannot be updated via API');
};
